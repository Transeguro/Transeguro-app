<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.transeguro.transeguroapp">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <protected-broadcast android:name="android.intent.action.MEDIA_MOUNTED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Splash screen -->
        <activity
            android:name=".SplashScreen"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Black.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".registrar" />
        <activity android:name=".Menu" />
        <activity android:name=".registrar_sugerencias" />
        <activity android:name=".datos_empresa" />
        <activity android:name=".foro" />
        <activity android:name=".lista_conductores" />
        <activity android:name=".rutas" />
        <activity android:name=".Historial" />
        <activity android:name=".hquejas" />
        <activity android:name=".hsugerencias" />
        <activity android:name=".registro_quejas" />
        <activity android:name=".perfil" />
        <activity android:name=".registro_quejas2" />
        <activity android:name=".registro_quejas3" />
        <activity android:name=".lista_sugerencias"></activity>
    </application>

</manifest>